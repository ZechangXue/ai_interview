# SCALA 技术路线与高质疑 FAQ（中文）

## 技术路线总览

| 模块/环节 | 我们在做什么（目的） | 用的具体方法（怎么做） |
|---|---|---|
| 总体路线 | 做一个“可解释、可复核”的胆囊手术训练评估流程 | 采用 **Segmentation → 状态建模 → 事件提议 → VLM判读 → 报告生成** 的分层流水线 |
| 前端（Web UI） | 给医生/培训者提供上传、进度查看、结果展示与报告导出界面 | **HTML + CSS + 原生 JavaScript**（模板在 `webapp/templates`，逻辑在 `webapp/static/*.js`），通过 `fetch` 调 FastAPI 接口；前端负责任务轮询、片段展示、报告渲染 |
| 后端（服务层） | 承接上传、任务调度、分割调用、状态导出与接口编排 | **FastAPI + Uvicorn + Pydantic**；使用 `BackgroundTasks`/`asyncio` 做异步后台任务，统一管理 job 状态和分割产物 |
| 视频处理层 | 把上传视频转成可稳定分析的数据流 | **OpenCV + NumPy** 做读写、降帧、帧处理、overlay 合成与样本帧输出 |
| 分割核心 | 把“手术画面”转成“结构化语义信号” | **PyTorch + segmentation_models_pytorch**，模型为 **FPN + EfficientNet-B4**，做 **13类语义分割** |
| 逐帧推理 | 在每帧识别组织/器械/出血等语义区域 | 帧级预测 class map（softmax+argmax），生成 mask/overlay，并统计每类像素面积 |
| 质量特征 | 给后续判断补充“画面质量”维度 | 计算 **clarity score**（Laplacian方差归一化）作为视野清晰度信号 |
| 帧状态建模 | 让后续模块不直接依赖原始图像 | 为每帧构建 state：可见性、bbox、pixel_count、几何特征、时间戳等，存入 JSON 友好结构 |
| 时序稳健性 | 避免单帧误检影响结论 | 使用持续时间门槛、孤立帧过滤、事件重叠抑制等时序后处理策略 |
| Calot关键事件提议 | 先筛出“最值得看”的关键窗口 | 基于规则组合（结构暴露、进展、出血、风险能量操作等）从 state 中提议关键事件片段 |
| VLM层（视觉-语言判读） | 对关键片段做高层语义与教学判断 | 通过 `webapp/vlm_api.py` 调 OpenAI 多模态能力；项目里主问答与视觉判读链路以 **GPT-4o 系列（含 mini）** 为主配置，按片段+状态联合判读 |
| 报告生成 | 输出可用于教学反馈的结构化结果 | 汇总可见性/安全性/效率等维度，生成证据驱动的训练评估报告 |
| 工程化能力 | 保证可用性和响应速度 | 模型预加载+缓存、后台异步任务、进度轮询、统一输出文件规范（demo视频/state JSON） |

---

## 高质疑 FAQ（10问10答）

### 1) 为什么不直接全交给 VLM，看视频就行？
**答：**纯 VLM 成本高、时序定位不稳、可解释性弱。我们先用 segmentation 产出结构化事件（可见性/血量/器械/清晰度），再让 VLM 在关键片段判断，准确率和可复核性更好。

### 2) 为什么要统一到 10 FPS？会不会丢信息？
**答：**这里目标是训练评估不是微操轨迹重建。10 FPS 能保住关键阶段信息，同时大幅降低计算量与抖动噪声，对事件稳定性更友好。

### 3) 为什么是 FPN + EfficientNet-B4，不是更大的模型？
**答：**这是精度、延迟、显存三者平衡点。我们需要批量可跑、可部署、可复现，而不是单例极限精度。

### 4) 为什么还要规则阈值事件，不全用学习型事件检测？
**答：**规则事件可解释、可审计、可快速迭代，特别适合临床教学场景。后续可在其上叠加学习型模块，而不是一开始黑盒到底。

### 5) 为什么要保存 `frame_state_cache`，不是实时算完就丢？
**答：**报告、问答、回放会重复访问同一帧状态。缓存后可以复用，显著减少二次开销和等待时间。

### 6) `main_demo_cv` 和 `main_demo_cv_share` 有什么本质差异？
**答：**算法链路一致，主要是入口、上传兼容性（如 webm）和展示定位不同。可以理解为“同引擎，不同门面”。

### 7) 为什么先 segmentation 提 clip，再让 VLM 看？
**答：**先把候选空间缩到“最可能有价值的片段”，VLM 才能把预算花在关键证据上，整体更稳定也更省钱。

### 8) 如果 segmentation 某一帧错了怎么办？
**答：**我们有时序连续性过滤、持续时间门槛、事件去重（重叠抑制），尽量避免单帧误检直接影响最终报告。

### 9) 为什么要同时输出 overlay 和 original 视频？
**答：**overlay 用于可解释展示，original 用于视觉模型/人工复核，避免“只看叠加图”带来的偏差。

### 10) 这条路线最大的工程收益是什么？
**答：**可解释、可复核、可扩展：分割层、事件层、VLM层可以独立优化，不会互相锁死。
